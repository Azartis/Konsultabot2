# Copy this file to `KonsultabotMobileNew/android/keystore.properties`
# and fill in your keystore details. NEVER commit the real file.
#
# To generate a release keystore:
# keytool -genkeypair -v -storetype PKCS12 `
#   -keystore konsultabot-release.keystore `
#   -alias konsultabot-release `
#   -keyalg RSA -keysize 2048 -validity 10000
#
# Move the generated .keystore file into android/keystores (create folder)
# and update the paths below.

storeFile=keystores/konsultabot-release.keystore
storePassword=Konsultabot123
keyAlias=konsultabot-release
keyPassword=Konsultabot123

